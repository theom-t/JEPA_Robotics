name: jepa_robotics
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - pyarrow      # Required for parquet dataset handling
  - h5py
  - opencv       # For video/image processing
  - tqdm
  - pyyaml
  - wandb        # For logging metrics
  - pip:
    # RTX 5090 Blackwell Support - using nightlies, no conda CUDA packages
    - jax[cuda12]
    - flax
    - optax
    - tensorflow-cpu
    - tensorflow-datasets
    - datasets
    - huggingface-hub
    - matplotlib
    - einops     # Essential for transformer/JEPA tensor manipulations
    - lerobot    # Hugging Face lerobot library for dataset streaming
    - pytest
    - pytest-cov
    - smac       # SMAC3 for hierarchical hyperparameter optimization
